DEFAULT = {

	#Alliances
	dynamic_country_name = {
		name = generic_alliance_two_members

		priority = 100
		
		# Scripted trigger, country-scope
		trigger = {
			exists = scope:actor
				scope:actor = {
					has_variable = confederations_num
					var:confederations_num = 1
					has_law = law_type:law_alliance
				}
		}
	}	
	dynamic_country_name = {
		name = generic_alliance_three_members

		priority = 100
		
		# Scripted trigger, country-scope
		trigger = {
			exists = scope:actor
				scope:actor = {
					has_variable = confederations_num
					var:confederations_num = 2
					has_law = law_type:law_alliance
				}
		}
	}	
	dynamic_country_name = {
		name = generic_alliance_four_members

		priority = 100
		
		# Scripted trigger, country-scope
		trigger = {
			exists = scope:actor
				scope:actor = {
					has_variable = confederations_num
					var:confederations_num = 3
					has_law = law_type:law_alliance
				}
		}
	}	
	dynamic_country_name = {
		name = generic_alliance_five_members

		priority = 100
		
		# Scripted trigger, country-scope
		trigger = {
			exists = scope:actor
				scope:actor = {
					has_variable = confederations_num
					var:confederations_num = 4
					has_law = law_type:law_alliance
				}
		}
	}
	dynamic_country_name = {
		name = generic_alliance_more_members

		priority = 100
		
		# Scripted trigger, country-scope
		trigger = {
			exists = scope:actor
				scope:actor = {
					has_variable = confederations_num
					var:confederations_num > 4
					has_law = law_type:law_alliance
				}
		}
	}
}